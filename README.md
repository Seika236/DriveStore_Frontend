# 🏎️ Drive Store Client

![Next.js](https://img.shields.io/badge/next.js-000000?style=for-the-badge&logo=nextdotjs&logoColor=white)
![React](https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB)
![Chakra UI](https://img.shields.io/badge/Chakra--UI-319795?style=for-the-badge&logo=chakra-ui&logoColor=white)
![TypeScript](https://img.shields.io/badge/typescript-%23007ACC.svg?style=for-the-badge&logo=typescript&logoColor=white)

Клиентская часть Fullstack-платформы **Drive Store**. Современное веб-приложение для покупки и продажи автомобилей, построенное с акцентом на производительность, строгую типизацию и масштабируемость.

---

## 🚀 Технологический стек

| Категория | Технологии |
| :--- | :--- |
| **Framework** | **Next.js 15** (App Router) |
| **UI Library** | **Chakra UI v3**  |
| **State Management** | **Zustand** + **Immer** (Immutable updates) |
| **Data Fetching** | **TanStack Query v5** + **Axios** |
| **Forms & Validation** | **React Hook Form** + **Zod** |
| **Real-time** | **Socket.io-client** |
| **Icons** | **React Icons** |

---

## 🏗 Архитектура проекта

Приложение реализовано с использованием **Lightweight Modular Architecture** (Feature-based подход). Это позволяет изолировать бизнес-логику и упрощает поддержку кода.

### Основные принципы:
* **App Router Groups:** Использование групп маршрутов `(protect)`, `(auth)`, `(dashboard)` для разделения уровней доступа и управления Layout-сетками без изменения URL.
* **Feature-First:** Весь функционал разбит на модули в папке `src/modules`. Каждый модуль (напр. `chat`, `listing`, `comment`) содержит собственную инфраструктуру (компоненты, хуки, сервисы, типы).
* **Server-Side Rendering (SSR):** Использование возможностей React Server Components для быстрой начальной загрузки и SEO.
* **Type Safety:** Глобальное использование TypeScript и валидация данных через Zod-схемы.

---

## 🛠 Ключевой функционал

* 🌗 **Theme Support:** Полная поддержка светлой и темной тем через возможности Chakra UI.
* 🔐 **Session-based Auth:** Безопасная авторизация на основе сессий с использованием Redis (на стороне бэкенда).
* 💬 **Real-time Chat:** Интерактивное взаимодействие в чатах через WebSockets.
* 📊 **Data Sync:** Синхронизация серверного состояния через TanStack Query (кэширование, инвалидация).
* 📝 **Type-safe Forms:** Валидация сложных форм на базе Zod-схем.
* 📱 **Responsive:** Адаптивная верстка, оптимизированная под диапазон от мобильных устройств до десктопов.

---

## Запуск локально

* Установите зависимости:
````bash
  npm install
  ````
* запуск в dev режиме:
````bash
  npm run dev
  ````
* запуск для production:
````bash
  npm run build
  npm run start
  ````

---

## Docker 🐳
Для работы с контейнерами используйте следующие команды:
#### Сборка образа
Эта команда соберет образ с именем next-app, используя файл ./Dockerfile.dev.
````bash
npm run docker-build
  ````
#### Запуск контейнера
Команда запустит проект в фоновом режиме на порту 3000.
````bash
npm run docker-run
  ````